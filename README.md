# Bitrix
Шаблон новостей 
articles.php это index.php просто нельзя было назвать его стандартно, потому что уже был такой файл у самого битрикс.
в export.xml находятся данные инфоблока, из которого берутся данные для шаблона. А в iblock находятся картинки, которые используются в инфоблоке
